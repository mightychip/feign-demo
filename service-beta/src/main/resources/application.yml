management:
  server:
    port: 8711

server:
  port: 8710
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:h2:mem:beta-db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop

